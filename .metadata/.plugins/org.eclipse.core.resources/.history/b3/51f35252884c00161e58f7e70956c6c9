/*
 * < 패키지 : 디렉토리 >
 *  평상 시 파일 저장할 때도 반드시 이 파일은 경로가 존재.
 *  마찬가지로 클래스 또한 반드시 경로가 존재하는데...
 *  파일 => 클래스
 *  폴더 => 패키지
 */

import 실험용패키지.* ;	// 실험용패키지의 모든 클래스를 포함하겠다.
//import : 외부 패키지의 클래스를 사용하기 위해 사용하는 키워드 
// => 가져오겠다. 
// import 패키지명; (X)
// 반드시 맨 마지막에는 클래스명이 와야 한다. 
public class _03_package {

	public static void main(String[] args) {
//	 	외부 패키지 소속의 클래스 객체를 생성!
//		패키지명.클래스명
		실험용패키지.Test a = new 실험용패키지.Test();
//		에러 : a.name = "둘리";
		
//		패키지로 나눈 클래스 단점 
//		1. 멤버에 접근이 불가능하다. ex) a.name = "둘리"(X)
//			=> 접근제한자
//		2. 클래스 접근이 불편하다. => import 키워드 사용
//		ex) 실험용패키지.Test a = new 실험용패키지.Test();
		
		Test b = new Test();
		
		
		
		
		
	}
}
